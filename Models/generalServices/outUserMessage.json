{
  "title": "outUserMessage",
  "x-stoplight": {
    "id": "rzvniowbtivjd"
  },
  "type": "object",
  "description": "The user messages displayed to FlightOps user. If the user message will include Error then the Error Object will be sent",
  "examples": [],
  "properties": {
    "type": {
      "type": "string",
      "description": "Type of message",
      "enum": [
        "info",
        "warning",
        "error",
        "success"
      ]
    },
    "title": {
      "type": "string",
      "description": "The user message title"
    },
    "message": {
      "type": "string",
      "description": "The user message text"
    },
    "timestamp": {
      "$ref": ""
    },
    "errors": {
      "type": "array",
      "items": {
        "$ref": "../General/ERROR.json"
      }
    },
    "airVehicleId": {
      "type": "string",
      "description": "If the message is related to specific air vehilce"
    },
    "missionId": {
      "type": "string",
      "description": "If the message is related to specific task"
    },
    "emitter": {
      "type": "string"
    },
    "id": {
      "type": "string",
      "description": "Message Id. If messages are grouped, the new message will arrive with the ID of the old message"
    }
  },
  "required": [
    "type",
    "title",
    "message",
    "timestamp",
    "id"
  ]
}